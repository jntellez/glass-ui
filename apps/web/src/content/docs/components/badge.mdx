---
title: Badge
description: Interactive badge used to display status, tags, or trigger actions in the interface.
---

import CommandTabs from "@/components/docs/CommandTabs.tsx"
import CodeBlock from "@/components/docs/CodeBlock.tsx"
import ComponentPreview from "@/components/docs/ComponentPreview.tsx"

<ComponentPreview name="badge-demo" client:load />

## Installation

<CommandTabs
  npm="npx @glass-ui-kit/cli add badge"
  pnpm="pnpm dlx @glass-ui-kit/cli add badge"
  yarn="yarn dlx @glass-ui-kit/cli add badge"
  bun="bunx @glass-ui-kit/cli add badge"
  client:load
/>

## Usage

<CodeBlock 
  lang="tsx" 
  code={`import { Badge } from "@/components/ui/badge"

  export default function App() {
    return (
      <Badge variant="soft">
        Beta
      </Badge>
    )
  }`}
/>

## Examples

### Weights

Demonstrates the built-in surface variants. Reach for `variant` before falling back to custom classes.

<ComponentPreview name="badge-weights" client:load />

### With Icon

You can easily incorporate icons inside the badge to provide more visual context.

<ComponentPreview name="badge-icon" client:load />

### With Spinner

A badge can display a loading state by including a spinner animation.

<ComponentPreview name="badge-spinner" client:load />

### Link

The badge can be rendered as a clickable element.

<ComponentPreview name="badge-link" client:load />

### Custom Colors

Use `className` as an escape hatch when you need custom semantic colors on top of the default badge surface.

<ComponentPreview name="badge-custom-colors" client:load />

## Styling

Badge now follows the same small prop-first direction as the other migrated form and action primitives. Start with `variant` and `size`, then use `className` for one-off overrides.

<CodeBlock 
  lang="tsx"
  code={`/* Variants */
  <Badge variant="default" />
  <Badge variant="soft" />
  <Badge variant="strong" />
  <Badge variant="transparent" />

  /* Sizes (defaults to md) */
  <Badge size="sm" />
  <Badge size="md" />
  <Badge size="lg" />

  /* Escape hatch */
  <Badge className="bg-emerald-50/50 text-emerald-700 dark:bg-emerald-950/50 dark:text-emerald-300" />`} 
/>

## API Reference

| Prop        | Type                                               |
| ----------- | -------------------------------------------------- |
| `variant`   | `"default" \| "soft" \| "strong" \| "transparent"` |
| `size`      | `"sm" \| "md" \| "lg"`                       |
| `className` | `string`                                           |
