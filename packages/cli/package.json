{
  "name": "@glass-ui-kit/cli",
  "version": "0.0.1",
  "description": "The official CLI for Glass UI",
  "private": false,
  "bin": {
    "glass-ui": "./dist/index.js"
  },
  "scripts": {
    "build": "bun build ./src/index.ts --compile --outfile dist/index.js",
    "dev": "bun run ./src/index.ts",
    "lint": "eslint src"
  },
  "dependencies": {
    "@glass-ui-kit/schema": "workspace:*",
    "commander": "^11.0.0",
    "chalk": "^5.0.0"
  },
  "devDependencies": {
    "@glass-ui-kit/tsconfig": "workspace:*",
    "bun-types": "latest",
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  }
}
